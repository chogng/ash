# Backend test fixtures

- `task_completed.json` and `task_failed.json` are synthetic backend responses for prompt, message, diff, and error extraction through `read_task`.
- The HTTPS server and certificates belong to [`http-test-support`](../../../http-test-support/README.md), shared by HTTP transport, operation, and backend protocol tests.
