// Language registrations belong to each LanguageService; built-in IDs are shared across editor consumers.
export const PLAINTEXT_LANGUAGE_ID = 'plaintext';
export const PLAINTEXT_EXTENSION = '.txt';
