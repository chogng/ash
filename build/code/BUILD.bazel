package(default_visibility = ["//visibility:public"])

filegroup(
    name = "python_sources",
    srcs = glob(["*.py"], exclude = ["test_*.py"]) + [
        "//build/runtime:python_sources",
        "//build/lib:archive_sources",
    ],
)
